import { ConvertOptionsPipe } from './convert-options.pipe';

describe('ConvertOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
