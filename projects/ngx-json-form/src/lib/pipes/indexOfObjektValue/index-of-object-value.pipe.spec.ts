import { IndexOfObjectValuePipe } from './index-of-object-value.pipe';

describe('IndexOfObjectValuePipe', () => {
  it('create an instance', () => {
    const pipe = new IndexOfObjectValuePipe();
    expect(pipe).toBeTruthy();
  });
});
